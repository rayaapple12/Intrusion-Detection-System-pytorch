# Intrusion-Detection-System-pytorch
A PyTorch-based Intrusion Detection System (IDS) using the UNSW-NB15 dataset. The project implements a Multi-Layer Perceptron (MLP) with data preprocessing, threshold tuning, and performance evaluation for network attack detection.
